# inherited_stream example

A Flutter example illustrating how to use `pkg:inherited_stream`

## Quick Start

To run the example, generate the platform(s) you wish to run on:

```sh
flutter create . --platforms=android,ios,linux,macos,web,windows
```

Then run the example:

```sh
flutter run
```
