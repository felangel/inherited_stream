name: inherited_stream
description: An inherited widget for Streams, which updates its dependencies when the stream emits data.
version: 1.1.0
repository: https://github.com/felangel/inherited_stream
issue_tracker: https://github.com/felangel/inherited_stream/issues
homepage: https://github.com/felangel/inherited_stream
documentation: https://github.com/felangel/inherited_stream

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
